# require 'spec_helper'

# describe Reaping do
#   let!(:reaping) { Reaping.new(type: "reaping", name: "reaping 1") }
#   end

# describe "#select_tribute" do
#   let!(:reaping) { Reaping.new(type: "reaping", name: "reaping 1") }
#   it "selects a tribute for the game" do
#     reaping.select_tribute("katnis everdeen")
#     expect(reaping.tributes.counts).to eq(1)
#   end
# end